<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Authentication</title>
    <!-- fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Josefin+Sans:ital,wght@0,100..700;1,100..700&display=swap" rel="stylesheet">

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Raleway:ital,wght@0,100..900;1,100..900&display=swap" rel="stylesheet">
    <!-- styling -->
    <style>
        body {
            height: 90vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }

        form {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;

            padding: 30px;

            border-radius: 5px;

            background-color: #86B6F6;
            color: #24282b;

            box-shadow: 0px 5px 5px 0px #24282b;
        }

        #passkey {
            margin-bottom: 45px;
            height: 30px;
            border: 0px;
            border-radius: 4px;

            font-family: "Raleway", sans-serif;
            font-optical-sizing: auto;
            font-weight: bold;
            font-style: normal;

            color: #24282b;
            background-color: #EEF5FF;
        }

        h2 {
            font-family: "Josefin Sans", sans-serif;
            color: #24282b;
            font-optical-sizing: auto;
            font-style: normal;
        }

        body, html {
            background-color: #EEF5FF;
        }

        #submit-button {
            width: 80px;
            height: 40px;
            border: 0px;
            border: 4px solid #176B87;
            border-radius: 4px;

            font-family: "Raleway", sans-serif;
            font-optical-sizing: auto;
            font-weight: bold;
            font-style: normal;

            background-color: #176B87;
            color: #EEF5FF;

            transition: background-color 2s, border 2s;
        }

        #submit-button:hover {
            background-color: #86B6F6;
        }

        @media(max-width: 425px) {
            h2 {
                font-size: 20px;
                text-align: center;
            }
        }
    </style>
</head>
<body>
    <form action="/blog/<%= index %>/edit" method="post">
        <% if (locals.sentence){ %>
            <label for="passkey"><h2><%= sentence %></h2></label><br><br>
        <% }else { %>
            <label for="passkey"><h2>Please enter your passkey to proceed further.</h2></label><br><br>
        <% } %>
        <input type="password" name="passkey" id="passkey" required>
        <input type="submit" id="submit-button" value="Submit">
    </form>
</body>
</html>